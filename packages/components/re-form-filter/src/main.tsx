import React from 'react';
import ReactDOM from 'react-dom/client';
import { FormFilter } from './index.ts';

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <FormFilter />
    </React.StrictMode>
);
